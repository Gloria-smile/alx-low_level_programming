#include <stdio.h>
/**
 * main - Entry point of the program
 *
 * This function prints the message "with proper grammar, but the outcome is
 * a piece of art," to the console using the printf function and returns 0
 * to indicate successful execution.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
printf("%s%s%s\n", "with proper grammar, ", "but the outcome is a ",
"piece of art,");
return (0);
}
